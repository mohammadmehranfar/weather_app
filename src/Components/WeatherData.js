import React from 'react';
import { fetchWeather } from '../api/fetchWeather';
const WeatherData = () => {
    return (
        <div>
            
        </div>
    );
};

export default WeatherData;